# BRD Agentic Framework — Dependencies

# Repo scanning
gitpython>=3.1.40
pydriller>=2.6

# Document parsing
unstructured>=0.12.0
pdfplumber>=0.10.0
python-docx>=1.1.0
openpyxl>=3.1.0

# Config parsing
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Streamlit UI
streamlit>=1.40.0
watchdog>=4.0.0

# Vector store & embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Multi-Provider LLM Support
anthropic>=0.40.0
openai>=1.0.0
google-generativeai>=0.3.0

# Graph
networkx>=3.0
# pyvis — not yet available for Python 3.13; install manually on Python <=3.12:
# pip install pyvis>=0.3.2

# API server
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0
requests>=2.32.0

# Uses the OS trust store for TLS — required behind corporate proxies that
# re-sign certificates with an internal CA that certifi does not carry.
truststore>=0.9.0

# Utilities
rich>=13.9.0
jsonschema>=4.23.0
tiktoken>=0.7.0

# Confluence sync (optional — enable when configured)
# atlassian-python-api>=3.41.0
